presubmits:
  cnrseng/test-infra:
  - name: test-presubmit
    branches:
    - master
    decorate: true
    always_run: true
    spec:
      containers:
      - image: alpine
        command: ["/bin/printenv"]
